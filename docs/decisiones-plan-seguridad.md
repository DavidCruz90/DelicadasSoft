# Decisiones tomadas durante el plan de seguridad de acceso

Plan: `docs/superpowers/plans/2026-09-17-plan-seguridad-acceso.md`. Ejecutado del 2026-09-17 al 2026-09-18.

Son las decisiones que el orquestador tomó sin consultar a Dave mientras construía, cada una con su motivo y lo que cuesta si es errónea. Se copian aquí desde el registro de trabajo, que no se guarda en el repositorio, para que ninguna quede escondida. Están en el orden en que se tomaron.

- **Antes de la Task 1** — Ruling: se ejecuta en `main`, sin worktree aparte — CLAUDE.md regla 2 exige `git push origin main` al terminar cada turno y el plan 1 se ejecutó igual. Costo si es errónea: `main` pasa por estados intermedios donde la API queda declarada pero aún sin guardia (entre Task 2 y Task 3); nada está desplegado, y se revierte con git.
- **Task 1** — Ruling: los documentos de cierre (ESTADO, BITACORA) se actualizan al terminar el plan, como manda la sección de Global Constraints del propio plan, no tarea por tarea. Los APRENDIZAJES sí se escriben cuando aparecen, por CLAUDE.md regla 2. Costo si es errónea: si la sesión se corta a mitad del plan, ESTADO queda apuntando al inicio de la fase; el ledger y git log lo reconstruyen.
- **Task 1** — Ruling: la prueba borrada ('un mesero inactivo no aparece en /api/estado') desaparece legítimamente porque /api/estado ya no devuelve la lista. Su intención se traslada a Task 4: GET /api/sesion/usuarios debe probar que inactivos y sin PIN no aparecen. Se lleva en el despacho de Task 4. Costo si es errónea: quedaría sin cubrir que un usuario inactivo pueda entrar; el barrido de rutas de Task 4 no lo detectaría.
- **Task 2** — Ruling: el mismo patrón muerto está en el código del plan para Task 3 (plan línea 1659, `crearSolicitud` reintenta ante choque de `dispositivo_codigo_pendiente_unico`): con el código del plan, un choque de código de 4 dígitos daría 500 en vez de reintentar. Se lleva al despacho de Task 3: extraer un ayudante compartido `esViolacionUnica(err, restriccion)` en `errores.ts` que mire `err.cause`, usarlo en `dispositivos.ts` y cambiar `usuarios.ts` para que lo use también (un solo patrón en el proyecto). Costo si es errónea: Task 3 toca `usuarios.ts`, fuera de su lista de archivos; es un cambio de una línea cubierto por la prueba de carrera de Task 2.
- **Task 2** — Ruling: el menor de verificarPin se corrige en Task 4, que es donde verificarPin empieza a decidir accesos (POST /api/sesion): exigir largo exacto de sal y clave antes de scrypt y añadir 'ab:abc' a la prueba de hash malformado. Se lleva en el despacho de Task 4. Costo si es errónea: entre Task 2 y Task 4 nadie llama verificarPin desde una ruta, así que no hay exposición.
- **Task 2** — minor (deferred): docs/APRENDIZAJES.md (entrada 2026-09-18) menciona "el ayudante compartido de errores.ts" que aún no existe. Se crea en Task 3 por el Ruling de Task 2; verificar tras Task 3 que la frase ya describe el código.
- **Task 2** — Ruling: la pestaña Usuarios, que nadie abrió en un navegador, se añade a "Para probar" de docs/fases/2-seguridad-de-acceso.md al terminar el plan, en un único recorrido junto con el resto de pantallas, y no ahora. Motivo: Dave pidió solo lo puntual y cuando corresponda; tras las Tasks 3 y 4 la pantalla de admin exigirá aparato autorizado y PIN, así que lo que probara hoy cambiaría. Task 7 la verifica en vivo con Chromium. Costo si es errónea: ninguno para Dave; si Task 7 no llegara a verificarla, quedaría sin mirar hasta el cierre.
- **Task 3** — despachada (claude-fable-5-1, BASE caa959c) — dispositivos y capa 1 del guardia; con el Ruling de esViolacionUnica en errores.ts, usado en dispositivos.ts y usuarios.ts, y prueba de choque por el camino real de Drizzle.
- **Task 3** — implementada DONE (commit 94e6917; 120 pruebas; typecheck y build limpios; sondeo desde IP de red, 18 caminos). Ruling de esViolacionUnica cumplido, prueba de choque vista fallar con la comprobación vieja. Desvío declarado: mensaje de 'autorizar sin nombre' resuelto a favor de la prueba del brief. Revisión despachada (claude-fable-5-1) con intento activo de romper el guardia (mayúsculas, codificación, doble barra, HEAD/OPTIONS) y verificación de fallo de arranque.
- **Task 3** — Ruling: C1 es un defecto del propio plan (el código del guardia venía así en el brief), no del implementador. Forma de la corrección: decidir por la declaración de la ruta que el router eligió — `acceso = req.routeOptions?.config?.acceso ?? (rutaCruda.startsWith('/api/') ? ADMIN : null); if (!acceso) return;` — y usar el prefijo crudo solo para las peticiones que no encontraron ruta (el 404 bajo /api/). Motivo: la spec dice que cada ruta declara su acceso; decidir por la declaración elimina la heurística de prefijo para toda ruta que exista, y Task 4 construye las capas 2 y 3 sobre ese mismo punto de entrada, así que hereda el arreglo. Costo si es errónea: si alguna ruta fuera de /api/ declarara acceso sin quererlo, pasaría a exigirlo; hoy ninguna lo hace, y el onRoute solo obliga a las de /api/.
- **Task 3** — Ruling: las pruebas de regresión de C1 van en tests/guardia.test.ts con las variantes codificadas del revisor por inject y la forma absoluta por un servidor real en puerto 0, y deben verse fallar contra el código actual antes del arreglo. El barrido de rutas de Task 4 debe incluir variantes codificadas; se lleva en su despacho.
- **Task 3** — Ruling: M1 y M2 de la re-revisión se resuelven en Task 4, que modifica guardia.ts y trae la prueba de barrido: la prueba por socket debe fallar con mensaje claro si no hay IPv4 de red, y el comentario se corrige. Costo si es errónea: ninguno de exposición; C1 queda cubierto también por la prueba unitaria de accesoExigido y las de inject.
- **Task 4** — Ruling: el brief de Task 4 (líneas 725-727) trae el onRequest con la comprobación vulnerable sobre req.url crudo, escrito antes del arreglo de C1. Se ordena NO transcribirla y construir las capas 2 y 3 a continuación de `accesoExigido(req)` del guardia actual (19cc528). Costo si es errónea: ninguno; es la misma decisión de C1 aplicada a las capas nuevas.
- **Task 4** — Ruling: no se espacia el UPDATE de dispositivo.ultimo_uso_en (M1 de la revisión de Task 3). Dos escrituras por petición en una cafetería son despreciables (HOT update) y espaciarlo exige SELECT + UPDATE condicional en el camino del guardia, que es complejidad nueva en el punto más sensible sin beneficio medible. Costo si es errónea: más versiones muertas en la tabla dispositivo, que el autovacuum absorbe.
- **Task 4** — Ruling: se acepta el desvío — el token de sesión va solo en la cookie HttpOnly y no en el cuerpo de POST /api/sesion, porque la spec §7 (corregida el 2026-09-17) lo dice así y la spec es la autoridad sobre el brief; además es más restrictivo y coherente con el token del aparato. Costo si es errónea: una línea en rutasSesion y una en la prueba.
- **Task 5** — Ruling (previo): el plan hace que POST /api/instalacion responda 201 { token, usuario } + cookie. Por el mismo motivo, el token de la instalación va solo en la cookie y la respuesta es { usuario, expira_en }. Se lleva en el despacho de Task 5; verificar también que Task 7 no lea el token del cuerpo.
- **Task 4** — Ruling: I-1 se corrige sin consultar a Dave — no es una decisión nueva sino la misma que Dave tomó el 2026-09-17 para el cambio de PIN (cortar al instante), aplicada a un caso más fuerte: desactivar. La spec ("desactivar corta el acceso en la petición siguiente") se cumple igual y además sin credencial latente. Costo si es errónea: al reactivar a alguien tiene que volver a escribir su PIN, que es lo esperable.
- **Task 4** — Ruling: la carrera de ingreso contra desactivación NO se aparca: se corrige antes de cerrar el plan, en la ola de arreglos de la revisión final, con la forma sugerida (abrir la sesión en una transacción que primero haga SELECT activo FROM usuario ... FOR KEY SHARE y rechace si es falso), prueba que fuerce la carrera y vista fallar. Es la misma clase de credencial latente que I-1. Se escribe en ESTADO.md para que no dependa de este registro. Costo si es errónea: ninguno de exposición mientras no esté instalado; es un cambio de pocas líneas.
- **Task 5** — despachada (claude-fable-5-1, BASE ff07042) — instalación y restablecer-pin; con el Ruling de token solo en cookie y la lista de ataques que hará la revisión.
- **Task 5** — Ruling: el 409 "El sistema ya está configurado" va sin `codigo`, y está bien — las Global Constraints del plan reservan `codigo` para 401/403/429 (errores de acceso); un 409 es regla de negocio y en este proyecto lleva solo `{ error }`. La pantalla de instalación de Task 7 distingue por el estado HTTP. Costo si es errónea: ampliar la lista cerrada en la spec y una línea en instalacion.ts.
- **Task 5** — Ruling: que restablecer-pin muestre el PIN mientras se teclea en modo interactivo se lleva como requisito del lanzador del plan 4 (la opción "Restablecer PIN de administrador" debe ocultar lo que se escribe), no se corrige ahora. Motivo: el comando solo corre con acceso físico a la PC de caja, y el plan 4 reemplaza este modo por el lanzador. Costo si es errónea: alguien mirando por encima del hombro durante una recuperación ve el PIN nuevo; el admin puede cambiarlo después desde la pantalla.
- **Task 5** — Ruling: I-1 se corrige en esLocal (no solo en exigirLocal): una petición es de la PC de caja solo si su IP es de bucle local Y su cabecera Host es un nombre de bucle local (127.0.0.1, localhost, [::1], con o sin puerto, sin distinguir mayúsculas); sin Host, no es local. Motivo: el mismo rebinding, ya instalado el sistema, saltaría la capa 1 para toda la API (leería /api/estado y la lista de usuarios, y probaría PINes bloqueando el ingreso local); arreglarlo en esLocal cubre instalación, capa 1 y el atado de sesiones a la PC en un solo punto. Es endurecimiento coherente con la intención de la spec ("la PC de caja" = estar sentado frente a ella), no una decisión nueva para Dave. Se refleja en la spec (5.2.6, regla 31 y restricción global). Costo si es errónea: si la PC de caja abriera el sistema por su IP de red en vez de localhost, dejaría de tratarse como local y pediría autorizar el aparato; el lanzador del plan 4 abre localhost.
- **Task 7** — Ruling (previo al despacho): el brief de Task 7 recarga /api/admin/dispositivos cada 30 s (línea 558) y todas las pantallas recargan con cada evento en vivo (useEventos). Cada petición con sesión renueva expira_en, así que admin y caja nunca vencerían por inactividad con la pantalla abierta: contradice lo aprobado por Dave ("se bloquea si nadie la toca"). Se decide: las peticiones automáticas (sondeos e recargas por evento) llevan `X-Automatica: 1`; el guardia las valida igual pero no renueva expira_en. Spec 4.3 actualizada. Es seguro: omitir la cabecera solo conserva el comportamiento anterior; ponerla solo acorta la propia sesión. Costo si es errónea: una pantalla que solo recarga sola vence y pide el PIN; lo esperable.
- **Task 7** — Ruling: por ese cambio, que toca el guardia del servidor (código de la Task 4) además de las pantallas, Task 7 se ejecuta con claude-fable-5-1 en lugar de claude-sonnet-5. CLAUDE.md regla 4: fable cuando hay que decidir o diseñar; ya no es transcribir. Costo si es errónea: más caro que Sonnet.
- **Task 7** — despachada (claude-fable-5-1 por el Ruling previo, BASE eb3eb8c) — pantallas + X-Automatica en guardia y api.ts.
- Revisión final (e32b5c6..f743ec7): la fase CUMPLE la spec; 0 críticos; 2 importantes; menores y textos desalineados. 167 pruebas, typecheck y build verificados por el revisor. Respalda todos los Rulings, con una corrección de forma.
- **Final** — I-1 (Importante) carrera entre entrar con PIN y desactivar O cambiar el PIN, confirmada en vivo en las dos variantes (201 y sesión viva; con el PIN viejo se abre una sesión que sobrevive al cambio). El Ruling de Task 4 proponía FOR KEY SHARE: el revisor midió que no espera a un UPDATE normal (cambiarPin toma FOR NO KEY UPDATE); hace falta FOR SHARE y recomprobar pin_hash.
- **Final** — Ruling: una sola ronda de arreglos de código con claude-fable-5-1 (concurrencia y seguridad): puntos 1 a 6 de la lista del revisor (carrera con FOR SHARE, cambio de rol cierra sesiones, /api/estado proyecta {id, abierta_en}, DELETE /api/sesion por req.sesionActual.id, restablecer-pin con base caída en español, dos comentarios). Los puntos 7 y 8 (spec, plan, ESTADO, BITACORA, APRENDIZAJES) son documentos del orquestador y los hace el orquestador. Costo si es errónea: ninguno; los textos no pasan por el implementador.
- **Final** — Ruling: el punto 8 del revisor pedía llenar "Para probar" de docs/fases/2 ahora. Se aplaza: después de lanzada la revisión final, Dave aprobó el cifrado de la red local (spec 2026-09-18-cifrado-red-local-design.md) y decidió probar la fase 2 una sola vez, al final, ya cifrada; la forma de entrar desde el celular cambia. "Para probar" se escribe al cerrar el plan de cifrado. El plan de seguridad se cierra; la fase 2 no.
- **Final** — Ruling: los 9 puntos de "lo que el plan 2 debe incorporar" del revisor se añaden a la sección final del plan de seguridad (orquestador), más: el lanzador oculta el PIN al teclearlo, abrir siempre por localhost, y cerrar las conexiones SSE del aparato al revocar (pendiente para el plan 3).
- **Final** — parked — carrera "cambiar el rol mientras se verifica el PIN": la relectura FOR SHARE compara activo y pin_hash pero no rol, y abrirSesion usa el rol viejo para expira_en; un mesero ascendido a admin en ese instante queda con sesión de admin sin vencimiento. Reproducido de forma determinista. Ruling: real y de seguridad, pero no se abre una segunda ronda (el método lo prohíbe tras la revisión final); se lleva como primer punto del plan de cifrado, que sigue dentro de la fase 2 y toca sesiones y cookies: traer rol en la relectura y tratar rol distinto como 401 (cambiar de rol es volver a entrar), con prueba de carrera vista fallar. Costo si es errónea: nada está instalado; la ventana es de ~50 ms y exige que el admin cambie el rol justo en ese instante.
- **Final** — parked — las pruebas de carrera no fijan que la relectura y el INSERT vayan en una misma transacción (sabotaje B pasa). Ruling: se lleva al mismo punto del plan de cifrado como mejora opcional de cobertura (disparador temporal con pg_sleep en el INSERT de sesion). Costo si es errónea: el comentario del código queda como única guarda de ese detalle.
- **Final** — parked — revocar un aparato mientras alguien entra desde él deja una sesión abierta huérfana atada al aparato revocado. Ruling: inofensiva (la capa 1 rechaza el aparato y no puede reautorizarse); limpieza junto con la de revocados en el plan 4. Costo si es errónea: una fila de más.
- **Final** — parked — restablecer-pin llama "base apagada" a cualquier error no propio. Ruling: dejar; es lo que sirve al dueño.
