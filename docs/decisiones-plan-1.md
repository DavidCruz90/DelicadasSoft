# Decisiones que tomé durante la ejecución del plan 1

Fecha: 2026-09-15 al 2026-09-17. Cada línea es una decisión tomada sin consultar a Dave, con su motivo y lo que costaría si estuviera equivocada. Están aquí para que Dave pueda revisarlas y revertir lo que no le convenga. El registro de trabajo del que salen era temporal y ya se borró; esto es lo que queda.

- Ruling: se trabaja directamente en `main`, sin worktree — CLAUDE.md regla 2 exige `git push origin main` al terminar cada bloque y Dave opera desde ese repo. Costo si me equivoco: historial de main con commits de trabajo en curso; se revierte con `git revert`.
- Ruling: Docker (OrbStack) estaba apagado; lo arranqué antes de empezar. Sin él, Task 1 no puede crear `cafeteria-pg`. Costo si me equivoco: ninguno, es reversible.
- Ruling (fila 9): Task 6 NO añade líneas `import` nuevas para símbolos ya importados en `app.ts`; amplía las líneas existentes (`import { existsSync, mkdirSync } from 'node:fs';`, `import { join, resolve } from 'node:path';`). Se incluye esta instrucción en el brief de Task 6. Costo si me equivoco: error de compilación evidente en el `typecheck` de esa tarea.
- Ruling (fila 18): `api.del` se conserva; es superficie de API que el plan 2 consume. Costo si me equivoco: una función muerta durante un plan.
- Task 1: Ruling: subo `drizzle-orm` 0.44.7 → 0.45.2 y `@fastify/static` 8.3.0 → ^10.1.3. Motivo: avisos de severidad alta verificados por mí con `npm audit --json`: inyección SQL por identificadores mal escapados en drizzle-orm <0.45.2, y evasión de guardas de ruta por path traversal en @fastify/static <=10.1.1. El segundo es serio aquí: el servidor sirve `/fotos/` y la web a toda la WiFi del local, así que un traversal expondría archivos de la PC de caja. Ambas correcciones existen y la superficie de API que usan los planes (pgTable, pgEnum, migrate, transacciones, `for('update')`, `root`/`prefix`/`decorateReply`/`index`/`wildcard`/`sendFile`) es estable entre esas versiones. Costo si me equivoco: alguna llamada cambia de firma y aparece en el typecheck o en las pruebas de las tareas 2, 3, 5 y 6, donde se corrige.
- Task 1: Ruling: NO subo `vitest` (3 → 5) ni `drizzle-kit`. Motivo: son dependencias de desarrollo que nunca llegan a la PC de caja; el aviso de vitest exige usar mocks de redirección, que no usamos, y el "arreglo" que npm propone para drizzle-kit es bajar a 0.18.1, una versión anterior a la que necesitamos. Costo si me equivoco: avisos de `npm audit` persistentes en desarrollo, sin efecto en producción.
- Task 2: Ruling (Importante 1, `encargo.pedido_id` sin clave foránea): aceptado, entra al arreglo. Es la única columna `*_id` del esquema sin `references`; el brief la omitió por la referencia circular pedido↔encargo. Drizzle resuelve esa circularidad con `AnyPgColumn`. Sin la clave foránea un encargo puede apuntar a un pedido inexistente, y los encargos mueven dinero. Costo si me equivoco: una migración de más.
- Task 2: Ruling (Importante 2, `actualizado_en` no se actualiza solo): aceptado, entra al arreglo con `$onUpdate`. Los planes 2 a 4 sí escriben `actualizado_en` a mano en cada update, así que el riesgo real es que alguna escritura futura lo olvide. Una línea en el helper `tiempos()`, sin migración, y el valor explícito sigue ganando cuando se pasa. Costo si me equivoco: ninguno, el comportamiento actual se conserva.
- Task 2: Ruling (Menores 5, 6, 7, 8): entran al mismo arreglo porque son baratos y previenen errores con dinero. 5: índice único que garantiza una sola fila en `configuracion`, si no una carrera al arrancar crearía dos y la segunda se ignoraría en silencio. 6: índice único parcial sobre `pedido.encargo_id`, evita entregar dos veces el mismo encargo si se reintenta la petición. 7: `UNIQUE` en `encargo.numero`, que es el número que ve el cliente. 8: la prueba de "una sola jornada abierta" usa `rejects.toThrow()` sin condición y pasaría con cualquier error; debe exigir el código 23505 de PostgreSQL. Costo si me equivoco: una migración algo más larga.
- Task 2: Ruling (Menores 3 y 4, restricciones CHECK en la base para montos, cantidades y coherencia de ítem libre): NO entran. La spec pone esas validaciones en el servidor (reglas 2, 7 y 16) y los planes 2 y 3 las implementan con sus pruebas. Un CHECK mal planteado rompería casos legítimos en producción: por ejemplo `movimiento_stock.cantidad` es un delta con signo y sí puede ser negativo. Costo si me equivoco: un fallo del servidor podría escribir un dato imposible que la base habría rechazado. Queda anotado en `docs/ESTADO.md`.
- Task 2: Ruling (`jornada.total_abonos_recibidos` y `total_abonos_devueltos` no figuran en la sección 4.2 de la spec): las columnas se quedan y corrijo la spec, no el código. La regla 12 exige que el cierre muestre esos datos y el plan 2 los congela al cerrar, así que las columnas hacen falta. Además simplifico la regla 12: "abonos recibidos hoy por método" pasa a ser un total único, porque lo único que afecta al arqueo es la parte en efectivo, y esa ya se informa aparte como saldo de la caja de encargos. Costo si me equivoco: si Dave quiere el desglose por método, es un cambio de reporte, barato.
- Task 3: Ruling (Importante 1): aceptado. Los errores propios de Fastify que ya traen `statusCode` 4xx salen como 500 genérico. Comprobado: un cuerpo JSON malformado responde 500 en vez de 400. En cuanto el plan 2 registre los POST, cualquier corte de red que trunque un cuerpo se vería como fallo del servidor y, con el registro del plan 4, ensuciaría el archivo de errores. Costo si me equivoco: ninguno, es estrechar un caso que hoy está mal.
- Task 3: Ruling (Importante 2): aceptado. Un suscriptor que lance una excepción rompe la petición que emitió el evento y deja sin avisar a las demás pantallas. En los planes 2 y 3 `emitir` se llama después de escribir en PostgreSQL: la venta ya estaría guardada pero el mesero vería "Error inesperado". Costo si me equivoco: ninguno.
- Task 3: Ruling (Importante 3): aceptado. `app.close()` no termina mientras haya una pantalla conectada al flujo de eventos, porque Fastify 5 solo cierra conexiones ociosas y una conexión de eventos nunca lo está. El plan 4 apaga el servidor y luego PostgreSQL al cerrar la ventana del lanzador; en una cafetería siempre habrá una pantalla abierta, así que el apagado nunca terminaría y la base quedaría mal cerrada. Se arregla ahora, que es donde vive el código. Costo si me equivoco: ninguno.
- Task 3: Ruling (Menores 1, 2, 4, 6): entran al mismo arreglo. 1: no hay ninguna prueba del manejador de errores, por eso el Importante 1 pasó inadvertido. 2: la ruta de eventos escribe en la respuesta cruda sin declarar que la toma por su cuenta. 4: si dos peticiones piden la configuración a la vez sin que exista la fila, la segunda choca con el índice único y devuelve 500; dos líneas lo evitan. 6: tipar el error del manejador en vez de dejarlo suelto. Costo si me equivoco: una ronda de arreglo algo más larga.
- Task 3: Ruling (Menor 3): NO entra ahora, se anota como restricción para los planes 2 a 4. Si esas rutas declaran esquemas de validación, los mensajes de Fastify salen en inglés y habría que traducirlos. Hoy no hay ningún esquema declarado, así que no incumple nada. Costo si me equivoco: algún mensaje en inglés llegaría al mesero y habría que traducirlo entonces.
- Task 3: Ruling (Menor 7, mover /api/estado a su propio módulo): NO entra. `app.ts` tiene 36 líneas y el patrón de registro por función ya está establecido; reestructurar ahora es trabajo sin beneficio. Sí llevo al brief de la tarea 6 la advertencia sobre el manejador de rutas no encontradas cuando empiecen a servirse archivos estáticos. Costo si me equivoco: una reestructuración más cara si `app.ts` crece mucho en el plan 2.
- Task 3: Ruling (desviación aceptada en el Importante 3): pedí resolver el cierre colgado con un conjunto de conexiones y un hook `onClose`, y el implementador demostró que esa vía no funciona: Fastify registra su propio hook de cierre del servidor en `preReady` y avvio los encola al frente, así que el hook nuestro nunca llega a tiempo. Cambió a `forceCloseConnections: true`, la alternativa que yo mismo había dejado planteada, y el revisor lo confirmó leyendo el código de Fastify 5.12.4 y midiendo: antes se colgaba más de 15 segundos, ahora cierra en 1 milisegundo con dos conexiones de eventos abiertas. Acepto. Costo si me equivoco: al apagar, una petición en vuelo se corta en seco en vez de terminar; en un punto de venta que se apaga al cerrar el local, es preferible a no apagar nunca.
- Task 4: Ruling (Importante 1): aceptado. Reactivar un mesero no comprueba el nombre, así que se puede acabar con dos "Carlos" activos. Reproducido en vivo. Como el sistema no usa contraseñas, esa lista es el único rastro de quién tomó cada pedido, y dos nombres iguales lo vuelven inútil. Costo si me equivoco: ninguno.
- Task 4: Ruling (Importante 2): aceptado. No hay validación de tipos: `cocina_activa: "true"` se guarda como falso en silencio, un `null` en un campo obligatorio devuelve 500, y un cuerpo que no sea objeto también. Es la capa que las tareas 5 a 7 y los planes 2 y 3 van a copiar, así que se arregla aquí antes de que se replique. Costo si me equivoco: alguna petición legítima con un tipo laxo empezaría a rechazarse; se vería en las pruebas.
- Task 4: Ruling (mover `validarUuid` a `errores.ts` como `exigirUuid`): aceptado. El revisor contó 26 usos de identificadores en ruta en los planes 2 y 3, más los que llegan en el cuerpo. Dejarlo en `meseros.ts` con el mensaje fijo "del mesero" garantiza que se copie seis veces. Se arregla también la línea de APRENDIZAJES que decía "replicar el patrón". Costo si me equivoco: un archivo compartido de más.
- Task 4: Ruling (Menor 1, índice único parcial sobre el nombre de mesero activo): aceptado, entra al arreglo con migración 0003. Es de la misma clase que los índices que ya acepté en la tarea 2: garantiza una identidad de negocio, no valida un rango, así que no contradice la decisión de dejar las validaciones de rango en el servidor. Además hace imposible el Importante 1 en lugar de solo comprobarlo, cerrando la ventana entre la comprobación y la escritura. Costo si me equivoco: una migración más y un error 23505 que hay que traducir a 409.
- Task 4: Ruling (Menor 2): aceptado. El nombre del local se guarda sin recortar espacios y aparecerá así en cada ticket; el símbolo de moneda admite texto vacío o larguísimo.
- Task 4: Ruling (Menores 3, 5 y 6): NO entran. 3: "Maria" y "María" se consideran distintos; es defendible y normalizar acentos añade una dependencia por un caso raro. 5: un PATCH con cuerpo vacío responde 200 y solo toca la fecha de modificación; inofensivo. 6: pasar `actualizado_en` a mano es redundante con el automático del esquema, pero no molesta. Costo si me equivoco: dos meseros con el mismo nombre escrito con y sin tilde; se distinguen a simple vista.
- Task 5: Ruling previo al despacho: el código de `catalogo.ts` en el brief valida con conversiones laxas (`Number(v)`, `Boolean(v)`, `String(v ?? '')`), la misma clase de defecto que el Importante 2 de la tarea 4. El implementador debe aplicar el patrón estricto ya establecido: `exigirObjeto` para el cuerpo, `exigirUuid` para identificadores en ruta y en cuerpo (`categoria_id`), `typeof` por campo, y traducción de 23505 a 409 donde aplique. Costo si me equivoco: el brief deja de ser literal en esas líneas; lo compensa que las pruebas del brief siguen pasando y se añaden las de tipos.
- Task 5: Ruling (Crítico 1): aceptado, se rechaza la desviación del implementador. Quitó la guarda del brief que impedía cambiar el stock por edición; probado en vivo: PATCH {controla_stock:true, stock_actual:999} deja el stock en 999 sin ningún movimiento. Se restaura la guarda y se añade la prueba. Costo si me equivoco: ninguno, es lo que el brief pedía.
- Task 5: Ruling (Importante 2, stock que nace o muere sin movimiento): aceptado. La spec 4.1 dice que cada cambio de stock queda registrado. Crear con stock inicial y activar el control generan movimiento `ajuste_manual` con motivo "Stock inicial"; desactivarlo genera movimiento con cantidad −stock_actual, resultante 0 y motivo "Control de stock desactivado". Costo si me equivoco: filas de más en movimiento_stock.
- Task 5: Ruling (Importantes 3 y 4, precio): aceptados. Precio fuera del rango de numeric(10,2) daba 500; más de dos decimales se redondeaba en silencio (4.005 → 4.00 por coma flotante). Se crea `exigirMonto` compartido en errores.ts: número o cadena numérica, finito, 0 ≤ valor ≤ 99 999 999.99, máximo 2 decimales, si no 400. Carry-forward a planes 2 y 3: todo monto de dinero (pagos, egresos, abonos, descuentos, propinas, ítems libres) usa `exigirMonto`. Costo si me equivoco: un precio con 3 decimales tecleado por error se rechaza en vez de redondearse; es lo deseable con dinero.
- Task 5: Ruling (Menores 1, 5, 6, 7-parcial, 9): aceptados, baratos. Campo multipart exigido `foto`; tipo de imagen comprobado por bytes mágicos además del mimetype; 413 y 406 con mensaje en español; al reemplazar una foto con otra extensión se borra el archivo anterior; corregir en BITACORA "8" por "7" pruebas nuevas.
- Task 5: Ruling (Menor 7-resto quitar foto, Menor 8 nombre de categoría único): NO entran. La spec no pide quitar fotos ni unicidad de categorías. Costo si me equivoco: una foto no se puede quitar, solo reemplazar; dos categorías con el mismo nombre se distinguen por orden.
- Task 5: Ruling (413 global dice "La foto supera el tamaño máximo de 5 MB"): aceptado, comprobado en vivo que ya aparece en un POST JSON de 1,2 MB sin foto. El mapa global pasa a "El contenido supera el tamaño máximo permitido"; se crea `ErrorTamano` (estado 413) en errores.ts y la ruta de foto lanza su propio mensaje de 5 MB. Coherente con el plan 3 tarea 6, que usará `ErrorTamano` para el límite de 1 MB del CSV. Costo si me equivoco: una clase de error más.
- Task 5: Ruling (editarProducto lee el producto fuera de la transacción y sin bloqueo, y el movimiento de desactivación usa ese stock leído): aceptado como Importante. Un ajuste de stock concurrente descuadraría el libro de movimientos. La lectura pasa dentro de la transacción con `for('update')`, igual que `ajustarStock`. Costo si me equivoco: ninguno.
- Task 5: Ruling (subida de foto borra la anterior antes de escribir la nueva): aceptado. Si la escritura falla, la base apuntaría a un archivo borrado. Escribir primero, actualizar la base, y borrar la anterior al final. Costo si me equivoco: ninguno.
- Task 5: Ruling: se abre ronda 3 solo para la prueba. Una prueba que no falla ante el defecto que dice cubrir no protege contra su regresión; se sustituye por la carrera forzada con una transacción externa que bloquea la fila. Costo si me equivoco: una ronda de más, pequeña.
- Task 6: Ruling previo: `tsconfig.servidor.json` usa `module: NodeNext`, que exige extensiones `.js` en imports relativos; el código usa imports sin extensión, así que `tsc -p tsconfig.servidor.json` dentro de `npm run build` probablemente falla. Nadie usa esa salida: desarrollo corre con tsx y el plan 4 empaqueta con esbuild. Si el implementador confirma el fallo, `build` pasa a `vite build` y `start` a `tsx src/servidor/index.ts`, sin añadir extensiones a todo el código. Costo si me equivoco: `npm start` depende de tsx en producción hasta el plan 4, que usa su propio lanzador.
- Task 6: Ruling (Importante 1, `useEstado` no recarga al reconectar y queda en "Cargando…" si falla la primera carga): aceptado. Demostrado: un cambio hecho durante el corte no aparece al volver la conexión. La spec 3.2 y 8 lo exigen. Costo si me equivoco: una petición más por reconexión.
- Task 6: Ruling (Importante 2, `api.ts` deja mensajes en inglés ante red caída o respuesta no JSON): aceptado. El mesero vería "Failed to fetch". Costo si me equivoco: ninguno.
- Task 6: Ruling (Menor 3, conexión muerta sin aviso): aceptado. Con WiFi que cae sin cerrar la conexión, `conectado` seguiría verdadero minutos y la franja "Sin conexión" de la spec 8 no aparecería. El latido pasa a evento con nombre y el cliente marca desconectado tras 60 s sin latido. Costo si me equivoco: un evento más cada 25 s.
- Task 6: Ruling (Menor 4, `/admin/` con barra final da 404): aceptado, barato. Costo si me equivoco: ninguno.
- Task 6: Ruling (eliminar `tsconfig.servidor.json`, huérfano tras el cambio de `build`): aceptado. Costo si me equivoco: recrearlo si algún día se compila el servidor con tsc.
- Task 6: Ruling (Menores 5 y 6, rutas dependientes del directorio actual y assets enumerados al arrancar): NO entran. El lanzador del plan 4 fija el directorio y usa CARPETA_RECURSOS; lo segundo solo afecta al desarrollo. Costo si me equivoco: reiniciar el servidor de desarrollo tras cada build.
- Task 7: Ruling previo: la verificación manual del paso 6 del brief se hace con Chromium headless de Playwright ya instalado en la máquina, con un script fuera del repositorio, en lugar de pedirla a Dave. Costo si me equivoco: algún detalle visual que un script no aprecia; Dave verá la pantalla al cerrar el plan.
- Task 7: Ruling (Importante 1, el aviso de error no se limpia): aceptado. Probado: tras un ajuste con "abc", el siguiente ajuste correcto deja el error viejo en pantalla junto al dato ya cambiado. El dueño vería un error sobre algo que sí se hizo.
- Task 7: Ruling (Importante 2, vacío convertido en 0 en silencio): aceptado. Aceptar el prompt de stock vacío pone el stock en 0 y registra el movimiento; crear un producto con control de stock y stock inicial vacío lo crea en 0; el umbral de stock bajo vacío se guarda como 0. Es pérdida de inventario sin aviso.
- Task 7: Ruling (Importante 3, propina con decimales imposible de guardar): aceptado. El input numérico sin `step` bloquea "12.5" con una burbuja del navegador y ni siquiera envía la petición, aunque el servidor acepta dos decimales.
- Task 7: Ruling (Menor 4, refresco en vivo): aceptado solo para Meseros, que es una lista. NO para Configuración: es un formulario y recargarlo con un evento pisaría lo que el dueño está escribiendo. Costo si me equivoco: la configuración abierta en dos ventanas no se sincroniza sola; se ve al recargar.
- Task 7: Ruling (Menor 7, el error de una acción de la tabla aparece arriba, lejos del botón): NO entra. Con pocos productos se ve; moverlo exige rediseñar la tabla. Anotado para el plan 2 si molesta.
- Ruling (A, `origen: 'apertura'` forjable desde admin): aceptado. Verificado en vivo: quedó un movimiento con origen apertura sin jornada. Contaminaría el reporte de apertura del plan 2.
- Ruling (B, pérdida de actualización en `editarMesero` y tres patrones de edición distintos): aceptado. Verificado: un cambio de estado concurrente pisa un renombre confirmado. Se fija un único patrón antes de que pedidos y cuentas lo copien.
- Ruling (C, validación triplicada): aceptado. `cuerpoComoObjeto` está copiado idéntico tres veces y hay tres funciones distintas para "texto obligatorio". Con veinte rutas más serían cinco copias.
- Ruling (D, dinero): aceptado. `exigirMonto` sin mínimo dejaría pasar pagos y abonos de 0 en el plan 2; y la propina sugerida redondea en silencio 12.555, justo lo que `exigirMonto` existe para impedir.
- Ruling (preparación): aceptados exportar `Tx` desde `db/conexion.ts` y compartir `NombreEvento` entre servidor y web. NO entra mover la consulta de jornada de `/api/estado` a un módulo: el módulo jornada nace en el plan 2 tarea 2, que hará ese cambio entonces. Costo si me equivoco: una consulta suelta en app.ts durante un plan.
- Ruling (menor, las pruebas escriben fotos en la carpeta del repositorio): aceptado, 63 archivos acumulados; si las pruebas corren en la PC de caja ensucian producción.
- Ruling (huecos de cobertura): entran los baratos que protegen dinero y reglas: pruebas unitarias de `errores.ts`, ajuste de stock en producto sin control, categoría inexistente al editar producto, y catálogo que excluye productos inactivos. NO entran las de 406 ni las de SSE más allá de cabeceras.
- Ruling (sin autenticación en /api/admin): no es defecto, es la decisión de diseño de no usar contraseñas. Se anota en ESTADO como riesgo aceptado: cualquier dispositivo de la WiFi puede cambiar precios; el stock deja rastro, los precios no.
